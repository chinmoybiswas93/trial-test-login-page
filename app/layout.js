import "./globals.css"
export const metadata = {
  title: "Sign Up FREE | Typeform",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
